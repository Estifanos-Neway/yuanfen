
function capitalize(string) {
    return string.charAt(0).toUpperCase() + string.slice(1);
}
function getSizedImageUrl({ url, size = [400, 533] }) {
    // const src = imageBuilder.image(url).size(size[0], size[1]).url();

}
export { capitalize }